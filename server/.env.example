# Only needed if you're calling a separately-hosted backend (server/ folder)
# instead of the built-in Vercel serverless function at /api/contact.
# Leave blank/unset when deploying everything to Vercel.
VITE_API_URL=
